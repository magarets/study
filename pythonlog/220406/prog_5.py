def checkpass(passwd):
    if(len(passwd) < 8):
        return 0
    else:
        passcheck = passcheck.split('')
        for i in passcheck:
            if(chr(i) > 64 and chr(i) < )

passwd = input("패스워드를 입력하세요")

while(flag != 1):
    flag = checkpass()

print("사용할 수 있습니다.")