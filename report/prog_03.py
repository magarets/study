sum = 0 # 누적변수이기때문에 초기화

for i in range(1, 101): # 1부터 100까지 반복하는 반복문
    if( i % 3 == 0): # 3의 배수를 구해야하기에 3으로 나눴을 때 나머지가 0인 수를 찾으면 됨
        sum += i # 3으로 나눴을때 나머지가 0인 i값을 sum에 값을 누적
print(f"1부터 100까지의 모든 3의 배수의 합은 {sum}입니다")